# MyRX mobile — see the parent CLAUDE.md

All mobile project context (tech stack, file tree, dev workflow with USB-connected device, conventions, gotchas, port status, cross-platform consistency rules) lives in the **root** CLAUDE.md one directory up, in the **Mobile Mirror** section:

```
../CLAUDE.md
```

(Absolute path: `C:\Users\motaz\OneDrive\Desktop\MyRX\CLAUDE.md`.)

There is no duplicate documentation here. Web (`./..`) and mobile (`./`) now live in the same repo, so a single source of truth keeps them in lockstep — when behavior diverges, the web file wins.

If you're a fresh agent landing in this directory, read `../CLAUDE.md` top-to-bottom (especially the **Mobile dev environment** subsection) before doing anything else.
